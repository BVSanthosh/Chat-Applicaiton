{
  "name": "chatcast-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "description": "Worker API. Deployed from the repo root — see the root wrangler.toml.",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.14"
  }
}
